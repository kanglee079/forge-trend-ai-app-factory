# Code Agent

Implemented in the MVP worker daemon as a deterministic Flutter scaffold plus a Codex CLI pass. Codex runs locally with `codex exec` inside the project workspace for customization and QA fix iterations. Aider and OpenHands are adapter placeholders.
