# Policy Agent

Check no copycat/trademark naming, no hardcoded keys, no excessive permissions, no minimum-functionality issue, no webview-only app, and privacy policy placeholder exists.

Return JSON with `risk`, `passed`, `issues`, and `required_changes`.
